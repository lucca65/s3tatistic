#!/usr/bin/env ruby

require 'rubygems'
require 'commander/import'

program :name, 's3tatistic'
program :version, '0.0.1'
program :description, 'Show statistics for your S3 buckets!'

command :buckets do |c|
  c.syntax = 's3tatistic buckets, [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'
  c.action do |args, options|
    # Do something or c.when_called S3tatistic::Commands::Buckets,
  end
end

command :creation, do |c|
  c.syntax = 's3tatistic creation, [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'
  c.action do |args, options|
    # Do something or c.when_called S3tatistic::Commands::Creation,
  end
end

command :files, do |c|
  c.syntax = 's3tatistic files, [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'
  c.action do |args, options|
    # Do something or c.when_called S3tatistic::Commands::Files,
  end
end

command :size, do |c|
  c.syntax = 's3tatistic size, [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'
  c.action do |args, options|
    # Do something or c.when_called S3tatistic::Commands::Size,
  end
end

command :recent do |c|
  c.syntax = 's3tatistic recent [options]'
  c.summary = ''
  c.description = ''
  c.example 'description', 'command example'
  c.option '--some-switch', 'Some switch that does something'
  c.action do |args, options|
    # Do something or c.when_called S3tatistic::Commands::Recent
  end
end
